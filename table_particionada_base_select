CREATE TABLE <NOME_TABELA>
   PARTITION BY LIST (<NOME_CAMPO>)(PARTITION P_<NOME_PARTICAO> VALUES (<VALOR_PARTICAO>)) 
   COMPRESS FOR ALL OPERATIONS AS
   SELECT CAMPOS, 
          TRUNC(SYSDATE) DT_PRC
   FROM   <NOME_TABELA_BASE> A;
                      
                      
